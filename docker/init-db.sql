-- Base de données PostgreSQL pour auth-service
-- Les tables seront créées automatiquement par Hibernate (ddl-auto: update)

-- Cette base est créée automatiquement par le conteneur PostgreSQL
-- via la variable d'environnement POSTGRES_DB=auth_db
