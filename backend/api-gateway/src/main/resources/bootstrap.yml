spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:8888
      username: configuser
      password: configpass
      fail-fast: false
      retry:
        max-attempts: 3
        initial-interval: 1000
